# 06 設定

実装: 2-05（ログアウト）/ 3-07（表示名・写真既定）。テーマ切替・招待コードは置かない（確定）。

モック: [settings.png](../wireframes/mocks/settings.png)

---

## `settings` 設定

| ルート | 親タブ | タブバー | ヘッダー | 主アクション |
|---|---|---|---|---|
| `/settings` | settings | 表示 | 「設定」 | なし（ログアウトは destructive） |

```
┌──────────────────────────────────┐
│               設定                │
├──────────────────────────────────┤
│ アカウント                         │  節見出し 13px muted
│ ┌ 行 ─────────────────────────┐  │
│ │ 表示名              オーナー ›│  │  タップでインライン編集
│ └──────────────────────────────┘  │
│ ┌ 行 ─────────────────────────┐  │
│ │ メール      user@example.com  │  │  表示のみ
│ └──────────────────────────────┘  │
│ 写真                              │
│ ┌ 行 ─────────────────────────┐  │
│ │ 写真にキャラを入れる（既定） [●]│  │  スイッチ。端末に保存
│ └──────────────────────────────┘  │
│ ┌ 行 ─────────────────────────┐  │
│ │ 色補正を掛ける（既定）      [●]│  │  スイッチ。端末に保存
│ └──────────────────────────────┘  │
│ セラー                            │
│ ┌ 行 ─────────────────────────┐  │
│ │ ラベルを自動で読み取る       [●]│  │  スイッチ。副文「写真を Cloudflare の AI に送ります」
│ └──────────────────────────────┘  │
│ ┌ 行 ─────────────────────────┐  │
│ │ ログアウト                    │  │  danger 文字
│ └──────────────────────────────┘  │
│ テーマは端末の外観設定に追従します   │  13px muted
│ alco-app 0.1.0                    │  13px muted
├──────────────────────────────────┤
│ ホーム  セラー  (記録)  ノート  設定*│
└──────────────────────────────────┘
```

### 要素表

| # | 要素 | 種別 | 内容・挙動 | データ / API |
|---|---|---|---|---|
| S1 | 表示名 | 行 → インライン Input | 1〜40 文字、空なら未設定。保存で `updateUser({ name })`。トースト「保存しました」 | Better Auth クライアント / `GET /api/me` |
| S2 | メール | 行（表示のみ） | 変更 UI なし | `GET /api/me` |
| S3 | 写真にキャラを入れる | スイッチ | `photo-edit` の合成トグルの **既定値**。`localStorage` に保存（サーバーに持たない） | クライアント |
| S4 | 色補正を掛ける | スイッチ | プリセット（`table` / `cellar`）の既定 ON/OFF。`localStorage` | クライアント |
| S5 | ラベルを自動で読み取る | スイッチ | ボトル追加時に写真を `POST /api/bottles/recognize`（Workers AI）へ送るか。副文で送信先を明示。`localStorage` `cellar.recognize`。既定 ON | クライアント |
| S6 | ログアウト | 行（danger） | 確認ダイアログ「ログアウトしますか」→ `signOut` → `/login` | Better Auth |
| S7 | 注記 | テキスト | テーマ追従の説明 + バージョン（ビルド時定数） | — |

### 状態

| 状態 | 表示 |
|---|---|
| ローディング | 行の枠 2 本 |
| `me` 取得失敗 | アカウント節に「読み込めませんでした」+ 再試行。ログアウトは使える |
| 表示名保存失敗 | 行内に汎用文、値は保持 |

キャラクターは出さない。

---

## 将来（ルートは作らない）

- 目標設定（週あたり上限、休肝日目標）— v1.x `settings-goals`
- アカウント削除 — 将来
- 利用規約 / プライバシー — Phase 8

---

## 受け入れチェック（2-05 / 3-07）

- [ ] 表示名インライン編集、メール表示のみ
- [ ] 写真 2 スイッチが `localStorage` に保存され、`photo-edit` の既定に反映
- [ ] 「ラベルを自動で読み取る」OFF でボトル追加時に AI へ送信されない（帯も出ない）
- [ ] ログアウトに確認、成功で `/login`
- [ ] テーマ切替・招待コードが無い
