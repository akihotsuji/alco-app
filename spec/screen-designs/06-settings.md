# 06 設定

実装: 2-05（ログアウト）/ 3-07（表示名・写真既定・**操作: 触感フィードバック / 動きを減らす**）/ 4-07（S5 ラベル読み取りを `bottle-new` に接続）/ 5.5（**表示: 外観** #62 / **記録: 現在地を記録する**）。招待コードは置かない（確定）。

モック: [settings.png](../wireframes/mocks/settings.png)

---

## `settings` 設定

| ルート | 親タブ | タブバー | ヘッダー | 主アクション |
|---|---|---|---|---|
| `/settings` | settings | 表示 | 「設定」 | なし（ログアウトは destructive） |

```
┌──────────────────────────────────┐
│               設定                │
├──────────────────────────────────┤
│ アカウント                         │  節見出し 13px muted
│ ┌ 行 ─────────────────────────┐  │
│ │ 表示名              オーナー ›│  │  タップでインライン編集
│ └──────────────────────────────┘  │
│ ┌ 行 ─────────────────────────┐  │
│ │ メール      user@example.com  │  │  表示のみ
│ └──────────────────────────────┘  │
│ 写真                              │
│ ┌ 行 ─────────────────────────┐  │
│ │ 写真にキャラを入れる（既定） [●]│  │  スイッチ。端末に保存
│ │ 新しい写真に合成します         │  │  過去の写真は変えない
│ └──────────────────────────────┘  │
│ セラー                            │
│ ┌ 行 ─────────────────────────┐  │
│ │ ラベルを自動で読み取る       [●]│  │  スイッチ。副文「写真を Cloudflare 経由の外部 AI に送ります」
│ └──────────────────────────────┘  │
│ 記録                              │
│ ┌ 行 ─────────────────────────┐  │
│ │ 現在地を記録する            [●]│  │  スイッチ。既定 ON。端末に保存
│ │ 新規の記録で現在地を残します   │  │  拒否時は端末設定を案内
│ │ 写真からの自動入力は           │  │  Cloudflare 経由の外部 AI。モデル名は出さない
│ │ Cloudflare 経由の外部 AI       │  │
│ └──────────────────────────────┘  │
│ 表示                              │
│ ┌ 行 ─────────────────────────┐  │
│ │ 外観                          │  │  チップ列 3 択（端末に従う* / ライト / ダーク）
│ │ (端末に従う) (ライト) (ダーク) │  │  端末に保存。選んだ瞬間に切り替わる
│ └──────────────────────────────┘  │
│ 操作                              │
│ ┌ 行 ─────────────────────────┐  │
│ │ 触感フィードバック           [ ]│  │  スイッチ。既定 OFF。副文「対応端末（Android）で有効」
│ └──────────────────────────────┘  │
│ ┌ 行 ─────────────────────────┐  │
│ │ 動きを減らす      端末の設定に従う ›│  │  2 択セグメント（端末の設定に従う / 常に減らす）
│ └──────────────────────────────┘  │
│ ┌ 行 ─────────────────────────┐  │
│ │ 使い方を見る                 ›│  │  初回ガイドを再実行
│ └──────────────────────────────┘  │
│ ┌ 行 ─────────────────────────┐  │
│ │ ログアウト                    │  │  danger 文字
│ └──────────────────────────────┘  │
│ このアプリ                         │
│ ┌ 行 ─────────────────────────┐  │
│ │ 利用規約                    ›│  │  → /terms?from=settings
│ └──────────────────────────────┘  │
│ ┌ 行 ─────────────────────────┐  │
│ │ プライバシーポリシー          ›│  │  → /privacy?from=settings
│ └──────────────────────────────┘  │
│ 酒のしおり 0.1.0                  │  13px muted
├──────────────────────────────────┤
│ ホーム  セラー  (記録)  ノート  設定*│
└──────────────────────────────────┘
```

### 要素表

| # | 要素 | 種別 | 内容・挙動 | データ / API |
|---|---|---|---|---|
| S1 | 表示名 | 行 → インライン Input | 1〜40 文字、空なら未設定。保存で `updateUser({ name })`。トースト「保存しました」 | Better Auth クライアント / `GET /api/me` |
| S2 | メール | 行（表示のみ） | 変更 UI なし | `GET /api/me` |
| S3 | 写真にキャラを入れる | スイッチ | 新規写真への合成の **既定値**。酒記録は撮影ごとに選ばずこの値を自動適用。過去写真は再合成しない。`localStorage`（サーバーに持たない） | クライアント |
| S4 | （廃止） | — | 色補正。処理・設定・旧 `photo.filter` は読まない | — |
| S5 | ラベルを自動で読み取る | スイッチ | ボトル追加時、および編集で新規写真を足したときに `POST /api/bottles/recognize` へ送るか。編集画面を開いただけでは送らない。副文で外部 AI である旨を明示（モデル名は出さない）。`localStorage` `cellar.recognize`。既定 ON。記録・ノートにはこのスイッチを広げない | クライアント |
| S6 | ログアウト | 行（danger） | 確認ダイアログ「ログアウトしますか」→ `signOut` → `/login` | Better Auth |
| S7 | 注記 | テキスト | 「酒のしおり」とバージョン（`PWA_NAME` とビルド時定数） | — |
| S8 | 触感フィードバック | スイッチ | `navigator.vibrate` による軽い振動（押下 10ms、保存成功 10-40-10ms）を有効にする。**既定 OFF**。`localStorage` `ui.haptic`。副文「対応端末（Android）で有効」。`navigator.vibrate` が無い端末（iOS）ではスイッチを無効にし副文「この端末では使えません」。ON にした瞬間に `haptic("light")` を 1 回鳴らして確認できる（[../motion-design.md](../motion-design.md) 6.5、X5） | クライアント |
| S9 | 動きを減らす | 2 択セグメント（`fieldset` / `legend`、行内） | 「端末の設定に従う」（既定）/「常に減らす」。native `radio`。タップでは radio にフォーカスしない（モバイルの IME 回避で `visualViewport` / `safe-area-inset-bottom` が変わり、タブバーが下半分を覆うのを防ぐ）。`localStorage` `ui.reduce-motion` = `system` / `always`。`always` または OS の `prefers-reduced-motion: reduce` で `AppShell` が `<html data-reduce-motion="1">` を付ける（[../motion-design.md](../motion-design.md) 6.8、X5）。OS 側が reduce のときは「端末の設定で動きが減っています」を副文に出す。ON のときキャラのまばたき・ウィンク・傾き・液面も止める | クライアント |
| S10 | 外観 | チップ列 3 択（`fieldset` / `legend`） | 「端末に従う」（既定）/「ライト」/「ダーク」。`localStorage` `ui.theme` = `system` / `light` / `dark`。`main.tsx` が React の描画より前に `lib/theme.ts` で解決し `<html data-theme="light|dark">` を付ける。CSS のトークンはこの属性だけを見る（属性が付く前の初回描画は `prefers-color-scheme` フォールバック）。`system` のときは OS の外観変更（`matchMedia` change）に追従する。別タブの変更は `storage` イベントで追従。選んだ瞬間に切り替わり、リロード後も維持 | クライアント |
| S11 | 使い方を見る | 行 | 「操作」節。タップで扇メニュー（記録 / セラー / ノート）を出す。機能名を選ぶとそのツアーの 1 歩目からスポットライト案内を始める（[08-first-run-guide.md](08-first-run-guide.md)、M-37）。完了の「ほかの使い方を見る」で着地したときはこの行までスクロールし、扇の選択肢がタブに隠れないようにする。健康情報の入力は置かない | クライアント |
| S12 | 現在地を記録する | スイッチ | 新規の `log-new` で現在地を 1 回取るか。**既定 ON**（従来どおり）。`localStorage` `logs.recordLocation`。副文「新規の記録で現在地を残します」。OFF ではフォームは位置情報を要求しない（保存済みの店名・座標は消さない）。ON にした瞬間に 1 回取得を試し、端末の許可ダイアログを出せる。`navigator.geolocation` が無い端末ではスイッチを無効にし副文「この端末では使えません」。Permissions API で `denied` のときは副文「端末の設定で位置情報を許可してください」。ブラウザ許可の取り消しはアプリからはできない | クライアント |
| S13 | 記録の外部 AI 注記 | テキスト | 「写真からの自動入力では、画像を Cloudflare 経由の外部 AI に送ります」。モデル名は出さない | — |
| S14 | 利用規約 | 行 | 「このアプリ」節。タップで `/terms?from=settings` | — |
| S15 | プライバシーポリシー | 行 | 「このアプリ」節。タップで `/privacy?from=settings` | — |

S8 / S9 / S11 は「操作」節として表示節の下、ログアウトの上に置く。S10 は「表示」節として記録節と操作節の間に置く。S12 は「記録」節としてセラー節と表示節の間に置く。S14 / S15 はログアウトの下、版表記の上。「このアプリ」節。いずれもサーバーに持たない（端末ごとの設定。S14 / S15 は静的ページへのリンク）。

### 状態

| 状態 | 表示 |
|---|---|
| ローディング | 行の枠 2 本 |
| `me` 取得失敗 | アカウント節に「読み込めませんでした」+ 再試行。ログアウトは使える |
| 表示名保存失敗 | 行内に汎用文、値は保持 |

キャラクターは出さない。

---

## 将来（ルートは作らない）

- 目標設定（週あたり上限、休肝日目標）— v1.x `settings-goals`
- アカウント削除 — 将来

---

## 受け入れチェック（2-05 / 3-07 / 5.5 #62 / 現在地の記録 / 8-01）

- [ ] 表示名インライン編集、メール表示のみ
- [ ] キャラ合成スイッチが `localStorage` に保存され、新規写真へ自動適用される。色補正の項目は無い
- [ ] 「ラベルを自動で読み取る」OFF でボトル追加時に AI へ送信されない（帯も出ない）
- [ ] 「触感フィードバック」が既定 OFF、ON で保存成功時に振動する（Android）。iOS ではスイッチ無効 + 「この端末では使えません」
- [ ] 「動きを減らす = 常に減らす」で `<html data-reduce-motion="1">` が付き、移動・水位線が消える。OS が reduce のときは副文が出る
- [ ] 動きを減らすのセグメントをタップしても画面がずれず、使い方を見る／ログアウトが隠れたままにならない
- [ ] ログアウトに確認、成功で `/login`
- [ ] 「外観」は既定「端末に従う」で OS に追従し、「ライト」/「ダーク」を選ぶと即時に切り替わり、リロード後も維持される（`<html data-theme>`）
- [ ] 「使い方を見る」で扇が開き、記録 / セラー / ノートのガイドを始められる。完了から来たときも選択肢が見える
- [ ] 「現在地を記録する」が既定 ON、OFF で新規記録が位置情報を要求しない。ON で許可ダイアログを出せる。非対応端末ではスイッチ無効
- [ ] 招待コードが無い
- [ ] 画面下の版表記は「酒のしおり」とバージョン
- [ ] 「このアプリ」に利用規約・プライバシーポリシーがあり、公開ページへ辿れる（8-01）
