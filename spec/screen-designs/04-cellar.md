# 04 セラー（陳列・貯蔵庫・追加・詳細・開栓）

実装: 4-02（追加・詳細・編集）/ 4-03（開栓・貯蔵庫・復元）/ 4-04（陳列）/ 4-06（切り抜き）/ 4-07（ラベル読み取り）/ 5.5（**まとめて追加** #56）。写真の撮影・編集・切り抜きは [07-photo-capture.md](07-photo-capture.md)。

モック: [cellar-shelf.png](../wireframes/mocks/cellar-shelf.png)（1 本ずつ）、[cellar-by-type.png](../wireframes/mocks/cellar-by-type.png)（種類ごと）、[cellar-archive.png](../wireframes/mocks/cellar-archive.png)、[cellar-new.png](../wireframes/mocks/cellar-new.png)（ラベル読み取り後）、[cellar-detail.png](../wireframes/mocks/cellar-detail.png)

2026-09-05（2 回目）のオーナー決定: 棚は **地色の上にガラス風の棚板**、ボトルは **切り抜き**（背景除去を MVP へ）。**種類ごと / 1 本ずつ**の表示切替。**ラベルの AI 読み取り**（Cloudflare Workers AI）はセラーのみ。

2026-09-06 追記: セラーの操作は **追加**と**開栓**。開栓で貯蔵庫へ移す。棚に残す「開栓済み」と、開栓時の 1 杯記録は持たない。1 杯は記録画面で付ける。

---

## 概念

```
   追加（撮って登録）                 開栓する
 ─────────────────►  セラー（棚）  ─────────────────►  貯蔵庫（アーカイブ）
                     sealed                              consumed
                          ▲                                 │
                          └────────── セラーに戻す（undo） ──┘
```

- **1 行 = 1 本**。登録時の本数 N はサーバーが N 行に展開する（上限 12）
- 棚にある本はすべて `sealed`。開栓済みのまま棚に残す状態は持たない
- **開栓する**は常に 1 本を貯蔵庫（`consumed`）へ移す。確認ダイアログは出さない。**記録は作らない**。1 杯は記録画面（`log-new` のボトル行）で付ける
- 写真は 2:3 に切り、`cellar` プリセットを掛け、**端末内で背景除去**して透過 WebP で保存する（`photos.kind = cutout`）。除去できないときは長方形 JPEG（`kind = photo`）にフォールバックし、棚では角 8px の写真として載せる
- ラベル読み取り: 撮影直後に **切り抜く前の 2:3 JPEG** を `POST /api/bottles/recognize` に送り、返ってきた候補を空のフィールドにだけ入れる。ユーザーが確認して保存する（自動保存しない）
- **まとめて追加**（`bottle-batch`）: 何本もあるときは 1 本ずつ撮って行に積み、最後に 1 回で棚に並べる。1 行 = 1 枚の写真 = 1 銘柄（本数 N 可）。API は `bottle-new` と同じ `POST /api/bottles` を行ごとに呼ぶ（サーバー変更なし）

---

## `bottle-list` セラー（陳列）

| ルート | 親タブ | タブバー | ヘッダー | 主アクション |
|---|---|---|---|---|
| `/cellar?view=one\|type` | cellar | 表示 | 左「貯蔵庫」（`archive`）「セラー 12 本」 右「まとめて追加」（`images`） | 追加（右下 FAB → `bottle-new?camera=1`） |

2 つの表示がある。切替は先頭の 2 択セグメント。最後の選択を `localStorage` に記憶（既定 **1 本ずつ**）。

#### 1 本ずつ（`view=one`）

```
┌──────────────────────────────────┐
│ [貯蔵庫]   セラー  12 本      [⧉]│
├──────────────────────────────────┤
│ [種類ごと|1本ずつ*] [🔍] [種類▼]  │  セグメント + フィルタ（横スクロール）
│                                  │
│    🍾        🍾        🍾         │  切り抜きボトル 100×150。棚板の上に立つ
│ ═══════════════════════════════ │  ガラス棚板 10px（--shelf-glass）
│  サンプル赤  モルト12  白ラベル    │  名前 13px（1 行省略）
│  2020       NV        NV          │  年。無ければ「NV」
│                                  │
│    🍾      ┌────┐     🍺          │  写真なしは種類のシルエット（inset 枠）
│            │(型) │                 │
│ ═══════════════════════════════ │
│  …                                │
│                              [+] │  右下 FAB（タブの上）
├──────────────────────────────────┤
│ ホーム  セラー* (記録)  ノート  設定│
└──────────────────────────────────┘
```

#### 種類ごと（`view=type`）

```
┌──────────────────────────────────┐
│ [貯蔵庫]   セラー  12 本      [⧉]│
├──────────────────────────────────┤
│ [種類ごと*|1本ずつ] [🔍]           │  種類フィルタは出さない（見出しが代わり）
│ ﾜｴﾞ ワイン 6本                     │  ゴースト見出し 44px（ボトルの背後）
│   🍾  🍾  🍾  🍾  🍾 →            │  横スクロール 1 段。72×120
│ ═══════════════════════════════ │  棚板は本数分の幅
│                                  │
│ ウイスキー 3本                     │
│   🥃  🥃  ┌─┐                     │
│ ══════════════                   │
│ 日本酒 2本 …                       │
│                              [+] │  右下 FAB（タブの上）
├──────────────────────────────────┤
│ ホーム  セラー* (記録)  ノート  設定│
└──────────────────────────────────┘
```

### 要素表

| # | 要素 | 種別 | 内容・挙動 | データ / API |
|---|---|---|---|---|
| C1 | 貯蔵庫 | Button 副（円、`archive`） | `/cellar/archive`。バッジは付けない | — |
| C2 | タイトル | テキスト | 「セラー」+ muted「12 本」（フィルタ前の在庫数） | `GET /api/bottles?view=cellar` の `totalCount` |
| C3 | 追加 | Button 副（円 52px、`plus`）。右下 FAB | `/cellar/new?camera=1`（撮影から始まる。キャンセルでフォーム）。タブバーの上 16px・右 20px。[00-common.md](00-common.md) 1.4 | — |
| C3b | まとめて追加 | Button 副（円、`images`）。ヘッダー右 | `/cellar/batch?camera=1`（撮影から始まる。`bottle-batch`）。ヘッダー右は 1 個（40px）のまま | — |
| C4 | 表示切替 | 2 択セグメント | 「種類ごと」「1 本ずつ」。URL `?view=` と `localStorage` に保存 | クライアント |
| C5 | 検索 | Chip（`search`）→ Input | タップで入力欄に展開。銘柄・生産者・品種の部分一致。300ms デバウンス | `q` |
| C6 | 種類フィルタ | Chip「種類 ▼」→ 7 種のダイアログ | 単一選択。選択中は「ワイン ×」。**種類ごと表示では非表示** | `drinkType` |
| C8 | 棚（1 本ずつ） | 棚（3 列 / 段） | `createdAt` 降順（新しい本が左上）。段ごとにガラス棚板。最後の段が 1〜2 本でも棚板は横一杯 | `items[]` |
| C9 | 棚（種類ごと） | 種類 × 1 段 | 種類は 7 種の定義順（ワイン → ビール → …）。在庫 0 の種類は出さない。段は横スクロール（`scroll-snap`）。ゴースト見出し「種類名 N 本」（N は `countsByType`）。棚板は本数分の幅（最小 1 本分、最大横一杯） | 種類ごとに `GET /api/bottles?view=cellar&drinkType=&limit=12`。見出しの本数は一覧応答の `countsByType` |
| C10 | ボトル | 切り抜き 100×150（種類ごとは 72×120）+ 名前 + サブ | `kind = cutout` は透過画像を `object-fit: contain`・下端を棚板に。`kind = photo` は角 8px の長方形。無ければ種類別シルエット SVG（`--muted` 線、inset-sm 枠） | `GET /api/photos/:id/content`、`thumbPhotoKind` |
| C11 | サブ行 | テキスト 12px | `vintage` があれば年、無ければ「NV」。種類ごと表示ではサブ行を出さず、タップで詳細 | `vintage` |
| C12 | ボトルタップ | — | `/cellar/:bottleId` | — |
| C13 | もっと読む | — | 1 本ずつ: 下端到達で `nextCursor` を自動取得（棚 2 段ずつ）。種類ごと: 段の右端到達でその種類を追加取得 | `cursor` |

状態フィルタ（未開栓 / 開栓済）は持たない。棚は `sealed` のみ。

### 状態

| 状態 | 表示 |
|---|---|
| ローディング | 棚 2 段分の枠（100×150 inset-sm ×6 + 棚板） |
| 空（在庫 0） | 棚板 1 本の上に **キャラ `surprised` 96px** が立つ。「ボトルはまだありません。撮って 1 本目を並べましょう」+ Button 主「ボトルを追加」。初回描画でキャラ・ボタンが 1 回現れ（M-26 / M-27）、棚板の上辺ハイライトが左 → 右へ 1 回走る（M-28） |
| フィルタ結果 0 | 棚板 1 本 + 「該当するボトルがありません」+ Chip「フィルタを解除」（キャラなし） |
| エラー | 「読み込めませんでした」+ 再試行 |
| 直前に操作したボトルがある（追加・復元の直後に一覧へ戻った、または開栓後に戻った） | そのボトルが載る（載っていた）段の棚板の上辺ハイライトが左 → 右へ 1 回走る（M-32 / M-12。`--dur-open`）。開栓後は加えてヘッダーの本数がカウントダウン（M-10）、復元で戻った本のタイルは上 6px から置かれるように現れる（M-11）。対象 ID は `history.state` で受け渡し、再生後に消す。復元（undo）直後の 2 秒ハイライトもこれに統一する（旧「primary の下線」は廃止） |

写真は `<img loading="lazy">`。同一オリジンの認可付き GET なので Cookie が付く。切り抜きは透過なので `<img>` の背景は地色のまま（白背景を敷かない）。

---

## `bottle-archive` 貯蔵庫（アーカイブ）

| ルート | 親タブ | タブバー | ヘッダー | 主アクション |
|---|---|---|---|---|
| `/cellar/archive` | cellar | 表示 | ← 「貯蔵庫 34 本」 | なし（閲覧） |

```
┌──────────────────────────────────┐
│ ←        貯蔵庫  34 本            │
├──────────────────────────────────┤
│ [🔍] [種類 ▼]                     │
│ 2026年9月                          │  月見出し（consumedOn の JST 月）
│  ┌────┐   ┌────┐   ┌────┐       │  写真は saturate(0.6) brightness(0.92)
│  │ 9/5│   │ 9/2│   │ 9/1│       │  右上に開栓日ピル
│  └────┘   └────┘   └────┘       │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  サンプル赤  …                     │
│ 2026年8月                          │
│  …                                │
├──────────────────────────────────┤
│ ホーム  セラー* (記録)  ノート  設定│
└──────────────────────────────────┘
```

| # | 要素 | 内容 | API |
|---|---|---|---|
| A1 | 見出し | 「貯蔵庫」+ 総数 | `GET /api/bottles?view=archive` |
| A2 | 月見出し | `consumedOn` の年月で区切る。降順 | `items[]` |
| A3 | ボトル | 棚と同じ切り抜き。`filter: saturate(0.5) brightness(0.9)`。右上に「9/5」ピル（開栓日 = `consumedOn`） | `consumedOn` |
| A4 | タップ | `/cellar/:bottleId`（詳細はアーカイブ表示になる） | — |

空: 「開栓したボトルはここに並びます」+ キャラ `default` 96px（主ボタンなし。戻るで棚へ）。

---

## `bottle-new` ボトルを追加

| ルート | 親タブ | タブバー | ヘッダー | 主アクション |
|---|---|---|---|---|
| `/cellar/new?camera=1` | cellar | **隠す** | ← 「ボトルを追加」 | 棚に並べる |

```
┌──────────────────────────────────┐
│ ←  ボトルを追加                    │
├──────────────────────────────────┤
│  🍾  撮り直す / 削除               │  撮影後: 切り抜き 100×150（撮影前は 2:3 の写真タイル）
│ [✦ ラベルから読み取りました。確認して保存] │  読み取り結果の帯（読み取り中は「読み取り中…」）
│ 銘柄名 *                           │
│ [ サンプル赤                 (AI)] │  1〜100。AI が入れた値には右端に AI 印
│ 種類 *                             │
│ [ワイン*][ビール][ウイスキー]…      │  AI が推定した種類を選択（印なし）
│ 本数                               │
│  [ − ]  1  [ ＋ ]                  │  1〜12。N 本を並べる
│ ビンテージ / 品種                   │  常時。AI が空欄へ入れる
│ [ NV          ] [ 例：カベルネ   ] │
│ 生産者 / 産地                       │
│ [             ] [ 例：シチリア   ] │
│ 詳細                           ▾  │  保管・購入・メモ。AI では開かない
│  ┌ 保管情報 ───────────────────┐ │
│  │ 保管日                       │ │  新規は登録日（JST）。変更可
│  │ [ 2026-09-08            📅 ] │ │  初期値は閉じたままでも保存
│  │ 初期値は登録日です。変更できます│ │
│  │ 保管場所                      │ │  新規の実値「自宅セラー」（プレースホルダではない）
│  │ [ 自宅セラー                ] │ │
│  └─────────────────────────────┘ │
│  ┌ 購入情報 任意 ───────────────┐ │  購入日は当日にしない
│  │ 購入日 / 購入価格 / 購入場所  │ │
│  └─────────────────────────────┘ │
│  ┌ メモ 任意 ──────────────────┐ │  3 行。既存の 2000 字とカウンター
│  │ [ 保管やボトルについてのメモ ] │ │
│  │                    0 / 2000 │ │
│  └─────────────────────────────┘ │
├──────────────────────────────────┤
│ [        棚に並べる（1 本）       ] │  ラベルに本数。キーボードと重ならない
└──────────────────────────────────┘
```

### 要素表

| # | 要素 | 種別 | 内容・挙動 | データ / API |
|---|---|---|---|---|
| B1 | 写真タイル | 写真タイル（2:3、左寄せ 100×150） | タップで撮影 → `photo-edit`（比率 2:3、プリセット `cellar`、**切り抜きトグル既定 ON**、**キャラ合成なし**）。下の「ライブラリから」で保存済み写真。`?camera=1` で自動起動（撮影から）。1 枚。撮影後は切り抜き（または長方形）のサムネ | `POST /api/photos`（`kind` は画像形式で判定） |
| B2 | ラベル読み取り帯 | 行（inset-sm、`sparkles`） | 写真確定直後に `POST /api/bottles/recognize`（切り抜く前の 2:3 JPEG）。状態: 「ラベルを読み取り中…」→「ラベルから読み取りました。内容を確認して保存してください」/「読み取れませんでした（手で入力してください）」。設定でラベル読み取り OFF のときは帯自体を出さない | api-design 4.5.3 |
| B3 | 銘柄名 | Input | 必須 1〜100。AI 候補は **空のときだけ**入れ、右端に `pill.ai`。ユーザーが編集すると印が消える | `name` |
| B4 | 種類 | Chip ×7 | 必須。初期はワイン。AI 候補で選択を変える（ユーザーが先に触っていれば変えない） | `drinkType` |
| B5 | 本数 | ステッパー | 1〜12。**同じ写真・属性で N 行**作る | `count` |
| B6 | ボトル情報 | 常時 | 銘柄・種類・本数の下。**ビンテージ / 品種 / 生産者 / 産地**を折りたたまず出す。AI が空欄へ入れる（確度 0.5 未満は捨てる。触った欄は上書きしない）。480px 以上だけビンテージ/品種を 2 列にしてよい | data-model 6.3 |
| B6b | 詳細 | 折りたたみ | 入口は「詳細」のまま。展開は **保管情報**（保管日・保管場所）→ **購入情報**（購入日・購入価格・購入場所）→ **メモ**。ラベルはすべて入力の上。入力高さ 48px。狭い画面は 1 列。480px 以上だけ購入日/価格を 2 列にしてよい。保管日の新規初期値は JST の登録日（未変更なら保存日の当日）。保管場所の新規初期値は「自宅セラー」（実値。詳細を閉じても保存）。購入日は自動設定しない。編集は保存済み値を出し、空欄を補完しない。購入情報 / メモに「任意」。**AI では開かない** | data-model 6.3 |
| B7 | 棚に並べる | Button 主 | 「棚に並べる（N 本）」。無効: 名前空 / 写真アップロード中。**読み取り中は無効にしない**（待たずに保存できる。結果は後から来ても空欄にだけ入る） | `POST /api/bottles` → 201 `{ items: [...] }` |

### 状態

| 状態 | 表示 |
|---|---|
| `camera=1` | `photo-edit` が先に開く。× で閉じてもフォームは残る（写真なし登録可。読み取りも起きない） |
| 切り抜き中 | サムネ位置に進捗リング。初回はモデル DL「初回のみ数十 MB を取得します」（[07-photo-capture.md](07-photo-capture.md)） |
| 読み取り中 | 帯にスピナー + 「ラベルを読み取り中…」（3〜8 秒） |
| 読み取り成功 | 帯が結果文に変わり、空欄に候補が入る。入った欄に AI 印 |
| 読み取り失敗 / 上限 | 帯「読み取れませんでした。手で入力してください」。フォームはそのまま使える |
| 成功 | 作成した最初の本の `bottle-detail` へ `replace`（B7 は M-04〜M-06 の loading / 即遷移 / 失敗の戻し）。トースト「棚に並べました」（`cheer` + 水面 M-25）。N ≥ 2 なら「棚に 3 本並べました」。その後 `bottle-list` に戻ると該当棚板に M-32 |
| 失敗 | インライン汎用文 |
| 戻る（未保存） | 入力ありなら確認。写真は削除 |

### バリデーション

| 項目 | 規則 |
|---|---|
| 銘柄名 | 1〜100 |
| 年 | 1800〜2100 の整数、または空 |
| 価格 | 0 以上の整数 |
| 本数 | 1〜12 |
| 購入日 | `YYYY-MM-DD`、未来不可。未入力のまま（当日にしない） |
| 保管日 | `YYYY-MM-DD`、未来不可。新規の初期値は登録日（JST）。未変更なら保存日の当日。購入日とは別項目 |
| 保管場所 | ≦100。新規の初期値は「自宅セラー」（実値） |
| AI 候補 | サーバーが Zod で検証済み（文字数・範囲・enum）。クライアントはさらに空欄にだけ入れる。確度 0.5 未満の候補は入れない |

---

## `bottle-batch` まとめて追加（連続撮影 → 一括登録）

| ルート | 親タブ | タブバー | ヘッダー | 主アクション |
|---|---|---|---|---|
| `/cellar/batch?camera=1` | cellar | **隠す** | ← 「まとめて追加」+ muted「3 本」 | 棚に並べる（N 本） |

何本もまとめて登録する導線（#56）。**撮る → 「使う」→ 裏で行に積まれる → すぐ次の撮影** を繰り返し、最後に 1 回で棚に並べる。切り抜き・アップロード・ラベル読み取りは裏で進む。ライブラリは **複数枚選択**して順に積む。`bottle-new` と同じ写真パイプライン（2:3・`cellar` プリセット・切り抜き既定 ON・キャラ合成なし）。

```
┌──────────────────────────────────┐
│ ←  まとめて追加             3 本   │
├──────────────────────────────────┤
│ ┌ 行 ───────────────────────────┐ │  outset カード。撮った順（上が古い）
│ │ ┌────┐ 銘柄名              [×]│ │  × = この行を外す（写真も削除）
│ │ │ 🍾 │ [ サンプル赤       (AI)] │ │  1〜100。AI 候補は空欄にだけ・AI 印
│ │ │64×96│ 種類                    │ │
│ │ └────┘ [ワイン*][ビール][ウイ…→ │ │  チップ ×7 横スクロール
│ │        本数 [−] 1 [＋]   詳細 ▾ │ │  1〜12 / 生産者・産地・ビンテージ・品種を開く
│ │ ✦ ラベルを読み取り中…           │ │  行ごとの読み取り帯（成功時は AI 印だけ）
│ └────────────────────────────────┘ │
│ ┌ 行 ───────────────────────────┐ │
│ │ ┌────┐ 銘柄名              [×]│ │
│ │ │    │ [                    ] │ │  空欄の行があると保存できない
│ │ │ ⟳  │ …                       │ │  アップロード中は進捗リング
│ │ └────┘                         │ │
│ └────────────────────────────────┘ │
│ [ 📷 次を撮る（あと 17 本） ]       │  Button 副（横一杯）。20 行で無効
│ ライブラリから（複数枚）              │  複数選択。同じ上限。選んだ順に裏で積む
├──────────────────────────────────┤
│ [       棚に並べる（3 本）        ] │  ラベルは全行の本数の合計
└──────────────────────────────────┘
```

### 要素表

| # | 要素 | 種別 | 内容・挙動 | データ / API |
|---|---|---|---|---|
| G1 | 行 | カード（outset） | 1 枚の写真 = 1 行。撮った順に下へ積む。**上限 20 行**。行を外す × は未紐付けの写真を `DELETE /api/photos/:id` してから消す | — |
| G2 | 写真 | サムネ 64×96（2:3、inset 枠） | 切り抜きは透過を地色の上に `contain`、長方形は `cover`。タップで `photo-edit` を再編集（同じ行に戻る）。アップロード中は進捗、失敗は「再試行」 | `POST /api/photos` |
| G3 | 銘柄名 | Input | 必須 1〜100。AI 候補は空のときだけ入れて AI 印。編集で印が消える（B3 と同じ） | `name` |
| G4 | 種類 | Chip ×7（横スクロール） | 初期ワイン。AI 候補で変える（先にユーザーが触っていれば変えない）（B4 と同じ） | `drinkType` |
| G5 | 本数 | ステッパー（行内） | 1〜12（B5 と同じ） | `count` |
| G6 | 詳細 | 折りたたみ（行内） | 生産者・産地（≦100）・ビンテージ（1800〜2100 / 空 = NV）・品種（≦100）。ビンテージはラベル左・入力右。AI がいずれかを入れたら自動で開く。購入日・価格・購入場所・保管場所・メモは持たない（あとで `bottle-edit`） | `producer`, `origin`, `vintage`, `variety` |
| G7 | 読み取り帯 | 行内テキスト（`sparkles`） | 行ごとに `POST /api/bottles/recognize`（切り抜く前の 2:3 JPEG）。「読み取り中…」→ 成功なら帯を消して AI 印だけ / 失敗「読み取れませんでした」。設定 OFF なら出さない。**読み取り中でも保存できる**（結果は後から空欄にだけ入る） | api-design 4.5 |
| G8 | 次を撮る | Button 副（横一杯、`camera`） | 撮影で `photo-edit` を開く。「使う」の **同じタップ**で次のカメラを開き（ユーザー操作を切らさない）、今の写真は裏で切り抜き・アップロード・読み取りして行に積む。OS の撮影キャンセルでループを終えフォームへ戻る。20 行で無効 + 「一度に 20 本までです」。`photo-edit` に「N 本を裏で処理しています」 | — |
| G8b | ライブラリから | テキストボタン（G8 の下） | **複数選択**（`input.multiple`）。選んだ枚数を上限まで、既定の切り抜き（中央・拡縮 1）で順に処理して行に積む。`photo-edit` は挟まない。あとから行のサムネで再編集できる | — |
| G9 | 棚に並べる | Button 主（SaveBar） | 「棚に並べる（N 本）」N = 全行の `count` 合計。無効: 行が 0 / 銘柄名が空の行がある / アップロード中・失敗の行がある。押下で行を **上から順に** `POST /api/bottles`（1 行 = 1 リクエスト）。全部成功 → `/cellar` へ `replace`、トースト「棚に N 本並べました」（`cheer` + M-25）、最初に作った本の段に M-32。一部失敗 → 成功した行だけ消し、失敗した行は残して行内に汎用文、上部に「N 行を並べられませんでした。もう一度お試しください」。もう一度押すと残りだけ送る | `POST /api/bottles` × 行数 |

### 状態

| 状態 | 表示 |
|---|---|
| `camera=1` | 到着直後に `photo-edit` が開く。× で閉じると行 0 の画面（「次を撮る」だけ。ラベルは「撮る」） |
| 行 0 | 「撮った写真がここに並びます」13px muted + G8。G9 は無効 |
| アップロード中 | G2 に進捗。G9 無効 |
| 送信中 | G9 が loading（M-04）。行の編集は無効 |
| 一部失敗 | 上記 G9 |
| 戻る（行あり） | 確認「入力を破棄しますか」「撮った写真も削除されます」。破棄で未紐付けの写真をすべて削除 |

キャラクターは出さない。

### バリデーション

`bottle-new` と同じ（銘柄名 1〜100、年 1800〜2100 / 空、本数 1〜12、AI 候補は空欄にだけ・確度 0.5 未満は捨てる）。

---

## `bottle-detail` ボトル詳細

| ルート | 親タブ | タブバー | ヘッダー | 主アクション |
|---|---|---|---|---|
| `/cellar/:bottleId` | cellar | 表示 | ← 銘柄名 右「編集」 | 開栓する（棚のとき）/ ノートを書く（貯蔵庫のとき） |

```
┌──────────────────────────────────┐
│ ←  サンプル赤                 編集 │
├──────────────────────────────────┤
│              🍾                   │  切り抜き、高さ最大 240px、中央
│ ═══════════════════════════════ │  ガラス棚板
│ [未開栓]  ワイン ・ 2020 ・ フランス│  状態ピル + 要約 13px
│                                  │
│ [          開栓する           ]   │  Button 主（棚のとき）
│                                  │
│ 銘柄名      サンプル赤              │  プロパティ（値が空の行は出さない）
│ 種類        ワイン                 │
│ ビンテージ  2020                   │
│ 品種        シャルドネ              │
│ 産地        フランス               │
│ 生産者      サンプル生産者          │
│ 購入日      2026-06-01             │
│ 価格        ¥3,800                 │
│ 購入場所    〇〇酒店               │
│ 保管日      2026-09-08             │
│ 保管場所    リビング               │
│ メモ        …                      │
│                                  │
│ ノート                    書く ›  │  このボトルのノート（最新 3 件 + すべて）
│  2026-08-01  ★4.5             ›  │
│                                  │
│ 記録                              │  このボトルの記録（最新 3 件。記録画面から紐付け）
│  9/5 13:05  125ml             ›  │  → log-edit
├──────────────────────────────────┤
│ ホーム  セラー* (記録)  ノート  設定│
└──────────────────────────────────┘
```

### 要素表

| # | 要素 | 種別 | 内容・挙動 | データ / API |
|---|---|---|---|---|
| T1 | 写真 | 切り抜き（高さ ≦240）+ ガラス棚板 | `kind = photo` なら 2:3 の長方形（高さ ≦300）。無ければボトル型 SVG。タップで全画面表示（ピンチ可）。**写真自体は動かさない** | `photos[0]` |
| T2 | 状態ピル + 要約 | ピル + テキスト | 棚は「未開栓」（inset-sm、muted 文字）。貯蔵庫は「開栓（9/5）」（`consumedOn`。outset-sm、primary 600）。要約は 種類 ・ 年 ・ 産地（あるものだけ）。ピルはこの画面内では切り替わらない（開栓・復元は遷移を伴う） | `status`, `consumedOn` |
| T3 | 開栓する | Button 主（棚のとき） | 確認なし。即時 `POST /api/bottles/:id/consume`。送信中は「開栓中」+ 水位線（M-04）。2xx で **詳細に留まる**（棚へ戻らない）。トーストとキャラを重ねず、開栓後シート（T3b）を出す。`haptic("success")`。失敗は水位線フェードアウト + 汎用文。記録・ノートは自動作成しない | `POST /api/bottles/:id/consume` |
| T3b | 開栓後シート | ボトムシート | 見出し「開栓しました」。補足「このボトルについて残しますか？」。操作は「飲んだ量を記録」「テイスティングを書く」「今はしない」の 3 つ（個別の「あとで」はボタン増を避けて置かない）。閉じる / 今はしないで両方見送り。片方だけ実行可。片方の保存後にもう片方のフォームは自動で開かない。フォームから戻っても再表示しない。残りの操作は T3c | — |
| T3c | このボトルについて | 行 2 つ | 「飲んだ量を記録」→ `/logs/new?bottleId=&from=detail`。「テイスティングを書く」→ `/notes/new?bottleId=&from=detail`。ノート節の一覧導線と混同しない | — |
| T4 | セラーに戻す | Button 副（貯蔵庫のとき） | T5 の上。即時 `POST /api/bottles/:id/restore` → 棚の詳細へ。トースト「セラーに戻しました」（`cheer` + M-25）。**記録は消さない** | `POST /api/bottles/:id/restore` |
| T5 | プロパティ | 行リスト | 順: 銘柄名 / 種類 / ビンテージ（無ければ「NV」）/ 品種 / 産地 / 生産者 / 購入日 / 価格 / 購入場所 / 保管日 / 保管場所 / メモ。銘柄名・種類・ビンテージ以外は値が空の行を出さない | data-model 6.3 |
| T6 | ノート節 | 見出し + 行 | 最新 3 件。「すべて（N）›」→ `/notes?bottleId=`。作成は T3c だけ（「書く ›」は置かない） | `GET /api/tasting-notes?bottleId=&limit=3` |
| T7 | 記録節 | 見出し + 行 | このボトルに紐付く記録 最新 3 件（記録画面から付けたもの）。行 → `log-edit`。無ければ節ごと出さない | `GET /api/drink-logs?bottleId=&limit=3` |
| T8 | 編集 | ヘッダー右 | `/cellar/:bottleId/edit` | — |

### 貯蔵庫（`consumed`）のとき

- T3 の「開栓する」は出さない。T3c と T4「セラーに戻す」を出す
- ピルは「開栓（9/5）」
- 写真は減彩しない（詳細では原色）

### 状態

| 状態 | 表示 |
|---|---|
| ローディング | 写真枠 + 見出し枠（静止。到着で M-29） |
| 404 | `not-found` |
| 開栓の送信中 | T3「開栓中」無効 + 水位線（M-04） |
| 開栓失敗 | 詳細に汎用文（オフラインなら X4）。画面は閉じない。水位線は M-06 で戻す |
| 開栓成功 | 詳細に留まる。T3b を 1 回出す。トースト・cheer は重ねない。あとで棚へ戻ったとき M-10 は出してよい |

### 開栓のメタファー（[../motion-design.md](../motion-design.md) 9 章。「少し凝った」段階を採用）

- 開栓 = **棚から貯蔵庫へ抜ける**。コルクも液体も描かない。ボトルの写真は動かさない
- 到着した `/cellar` で 3 つが同時に始まり 300ms で静止する（M-10）: ヘッダー「セラー N 本」が N → N−1 にカウント、抜けた本があった段の棚板の上辺 1px の中を明帯（幅 24%、`--shelf-glass-edge`）が左 → 右へ 1 回走る、トースト（M-23 / M-25）。隣の本は再描画で自然に詰まる（FLIP はしない）
- 取り消しで本が戻るときは、そのタイルが上 6px から不透明 0 → 1 で **置かれる**ように現れ（M-11、`--dur-enter`）、段の棚板に明帯（M-32）、本数が N に戻る
- 対象の段は `history.state.left = { bottleId, createdAt }` から `createdAt` 降順の順位で求める（3 列 / 段。480px 以上は 4 列）
- reduced motion では本数の即時差し替えとトーストの不透明度フェードだけ

### 開栓の成功後

1. 開栓 API が完了してから T3b を出す。記録量の推測保存はしない
2. シートを閉じるだけで記録もノートも作らない。キャンセルしても開栓は戻さない（戻すのは T4）
3. フォームへ進んだあとの戻りはボトル詳細。同じ案内は再表示しない

---

## `bottle-edit` ボトルを編集

`/cellar/:bottleId/edit`。`bottle-new` と同じレイアウト。差分:

- 本数ステッパーは無い（1 行 = 1 本）
- 状態は編集できない（詳細の「開栓する」「セラーに戻す」で操作）。開栓日は詳細のピル（`consumedOn`）
- 写真の付け替え: 削除 → 撮る。`PATCH` の `photoIds`
- 保存 → `PATCH /api/bottles/:id` → 詳細
- 下部テキストボタン「このボトルを削除」→ 確認ダイアログ「ノートは残ります。記録のボトル名は残ります」（danger）→ `DELETE` → `/cellar`。**貯蔵庫の本も削除できる**

---

## 陳列の写真（4-04 / 4-06 の実装メモ）

| 項目 | 値 |
|---|---|
| 入力 | `photo-edit` の出力。`kind = cutout`: 透過 WebP（2:3 キャンバス、ボトルは下端揃え、長辺 1280）。`kind = photo`: JPEG 2:3（`cellar` プリセット済み） |
| 棚のサムネ | 100×150（種類ごとは 72×120）。cutout は `object-fit: contain` で下端を棚板の上辺に。photo は `cover`・角 8px |
| 棚板 | 高さ `--shelf-h` 10px、`--shelf-glass`、上辺 1px `--shelf-glass-edge`、`--shelf-glass-shadow`、`backdrop-filter: blur(2px)`（未対応なら省略）。段の間隔 24px |
| 棚板ハイライト（M-12 / M-28 / M-32） | `.shelf-board::before` に幅 24% の明帯（`--shelf-glass-edge`）を置き、`mask` で上辺 1px に限定して `translateX(-100% → 500%)` を `--dur-open` 300ms `--ease-out` で 1 回。`data-highlight` 属性で発火。reduced motion では無し。`Shelf` 共通部品に持つ（一覧・空状態・開栓後の到着で同じ）。ボトルの出現（M-11）は `BottleTile` の `data-enter` |
| 段の構成（1 本ずつ） | 390px で 3 列（列幅 = (390 − 40 − 16×2) / 3 ≒ 106px）。480px 以上で 4 列 |
| 段の構成（種類ごと） | 1 種類 = 1 段、横スクロール。列幅 72px、間隔 22px。ゴースト見出し 44px / 700 `--shelf-ghost`、段の左上 −18px に重ねる |
| 落ち影 | cutout の足元に楕円の影（幅 80%、高さ 6px、黒 25%）を画像側に焼き込む（`photo-edit`。棚板の上に立って見える） |
| 写真なし | 種類別ボトル型 SVG（ワイン=肩なだらか、ビール=缶、ウイスキー=角瓶、日本酒=一升瓶、焼酎=瓶、カクテル=グラス、その他=汎用瓶）。線は `--muted`、inset-sm の枠 |
| 背景除去 | ブラウザ WASM（`onnxruntime-web` + U2-Net-P。同一オリジン `/models/`）。初回にモデルを DL（Cache API に保存）。処理 2〜8 秒。失敗・未対応（WASM SIMD なし、メモリ不足）では `kind = photo` にフォールバック。詳細は [07-photo-capture.md](07-photo-capture.md) |

---

## 受け入れチェック（4-02 / 4-03 / 4-04 / 5.5 #56）

- [ ] `/cellar` が **ガラス棚板 + 切り抜きボトル**。1 本ずつ = 3 列、名前とビンテージ（無ければ NV）
- [ ] 表示切替「種類ごと / 1 本ずつ」が URL と `localStorage` に保存され、種類ごとはゴースト見出し + 横スクロール 1 段
- [ ] `kind = photo` は角 8px の長方形、写真なしは種類別シルエット。空状態は棚板 + `surprised` 96px + 「ボトルを追加」
- [ ] 検索・種類フィルタが動き、状態フィルタは無く、貯蔵庫（`consumed`）は棚に出ない
- [ ] 右下 FAB「+」→ 撮影から始まり、× でフォームに戻れる。写真タイル下に「ライブラリから」。`cellar` プリセット、切り抜き既定 ON、キャラ合成トグルなし
- [ ] 写真確定でラベル読み取りが走り、空欄にだけ候補が入って AI 印が付く。失敗時もフォームは使える。設定 OFF で帯が出ない
- [ ] 本数 N で N 行。「棚に並べる（N 本）」
- [ ] 棚ヘッダー右は「まとめて追加」だけ。追加は右下 FAB。まとめて追加は撮影から始まり、「使う」で行が裏に積まれ、同じタップで次の撮影が開く（20 行で無効）。「ライブラリから」は複数枚選択して順に積める
- [ ] まとめて追加: 行ごとに銘柄名（AI 印）・種類・本数・詳細（生産者 / 産地 / ビンテージ / 品種）・読み取り帯。× で行と写真が消える
- [ ] まとめて追加: 「棚に並べる（N 本）」は本数合計。全成功で `/cellar` + トースト「棚に N 本並べました」。一部失敗で失敗行だけ残る。戻るで確認 + 写真削除
- [ ] 詳細: 棚は主「開栓する」のみ。開栓成功後は詳細に留まり T3b を出す。貯蔵庫は T3c「飲んだ量を記録」「テイスティングを書く」と「セラーに戻す」。ノート節に「書く ›」は置かない。プロパティに銘柄名・種類・ビンテージ・品種・産地など（空行は出さない）。記録があれば記録節
- [ ] 単体追加: ビンテージ / 品種 / 生産者 / 産地が常時表示。詳細は保管・購入・メモ。AI で詳細は開かない
- [ ] 詳細の展開は保管情報 → 購入情報 → メモ。ラベルは入力の上。狭い画面は 1 列
- [ ] 新規の保管日は登録日（JST）、保管場所は「自宅セラー」。詳細を閉じても保存される。購入日は当日にしない
- [ ] 編集では保存済みの保管日・保管場所を維持し、空欄を今日や「自宅セラー」で補完しない
- [ ] 開栓は確認なし。送信中は「開栓中」+ 水位線、2xx で詳細に留まり T3b を出す。トーストとキャラは重ねない。記録・ノートは自動作成しない。あとで棚へ戻ったとき M-10 は出してよい
- [ ] 開栓後の `/cellar`: 本数がカウントダウンし、抜けた段の棚板に明帯が 1 回走り、300ms で静止する（M-10）。取り消しで本が上 6px から置かれるように戻り、棚板に明帯（M-11 / M-32）
- [ ] 一覧: 追加・復元の直後に戻ると該当棚板に明帯が 1 回（M-32）。空状態でキャラ・ボタンの 1 回出現と棚板の明帯（M-26〜M-28）
- [ ] reduced motion で開栓後の演出が本数の即時差し替えとトーストのフェードだけになる
- [ ] 貯蔵庫: 月見出し、減彩、開栓日ピル。詳細で T3c と「セラーに戻す」
- [ ] `bottle-edit` に削除（確認。ノート・記録は残る）
- [ ] 他人の `:bottleId` は `not-found`
