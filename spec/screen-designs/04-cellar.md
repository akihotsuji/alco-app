# 04 セラー（陳列・貯蔵庫・追加・詳細・消費）

実装: 4-02（追加・詳細・編集）/ 4-03（消費・貯蔵庫・開栓）/ 4-04（陳列と写真加工）。写真の撮影・編集は [07-photo-capture.md](07-photo-capture.md)。

モック: [cellar-shelf.png](../wireframes/mocks/cellar-shelf.png)、[cellar-archive.png](../wireframes/mocks/cellar-archive.png)、[cellar-new.png](../wireframes/mocks/cellar-new.png)、[cellar-detail.png](../wireframes/mocks/cellar-detail.png)、[cellar-consume.png](../wireframes/mocks/cellar-consume.png)

---

## 概念

```
   追加（撮って登録）                 消費（飲み終えた）
 ─────────────────►  セラー（棚）  ─────────────────►  貯蔵庫（アーカイブ）
                     sealed / opened                    consumed
                          ▲                                 │
                          └────────── セラーに戻す（undo） ──┘
                                            │
                              その日の記録に 1 杯を追加（任意）→ log-day
```

- **1 行 = 1 本**。登録時の本数 N はサーバーが N 行に展開する（上限 12）
- 開栓（`sealed → opened`）はサブ操作。棚に残る
- 消費は常に 1 本。ダイアログで「今日の記録に 1 杯追加」を選べる（既定 ON）
- 写真の切り抜き（背景除去）は **v1.x**。MVP は 2:3 に切り、色補正プリセット `cellar` を掛け、棚板に載せる

---

## `bottle-list` セラー（陳列）

| ルート | 親タブ | タブバー | ヘッダー | 主アクション |
|---|---|---|---|---|
| `/cellar` | cellar | 表示 | 左「貯蔵庫」（`archive`）「セラー 12 本」 右「追加」（`plus`） | 追加（`bottle-new?camera=1`） |

```
┌──────────────────────────────────┐
│ [貯蔵庫]    セラー  12 本      [+]│
├──────────────────────────────────┤
│ [🔍] [種類 ▼] [未開栓] [開栓済]   │  フィルタ。検索はタップで入力欄に展開
│                                  │
│  ┌────┐   ┌────┐   ┌────┐       │
│  │写真 │   │写真 │   │写真 │       │  2:3、100×150。棚板の上に立つ
│  │2:3 │   │    │   │    │       │
│  └────┘   └────┘   └────┘       │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │  棚板 14px（--shelf-rail）
│  サンプル赤  モルト12  白ラベル    │  名前 13px（1 行省略）
│  2020       開栓 ●    NV          │  年 or 開栓ピル
│                                  │
│  ┌────┐   ┌────┐                 │
│  │    │   │(型) │                 │  写真なしは種類のボトル型（SVG）
│  └────┘   └────┘                 │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  …                                │
├──────────────────────────────────┤
│ ホーム  セラー* (記録)  ノート  設定│
└──────────────────────────────────┘
```

### 要素表

| # | 要素 | 種別 | 内容・挙動 | データ / API |
|---|---|---|---|---|
| C1 | 貯蔵庫 | Button 副（円、`archive`） | `/cellar/archive`。バッジは付けない | — |
| C2 | タイトル | テキスト | 「セラー」+ muted「12 本」（フィルタ前の在庫数） | `GET /api/bottles?view=cellar` の `totalCount` |
| C3 | 追加 | Button 副（円、`plus`） | `/cellar/new?camera=1`（撮影から始まる。キャンセルでフォーム） | — |
| C4 | 検索 | Chip（`search`）→ Input | タップで入力欄に展開。銘柄・生産者部分一致。300ms デバウンス | `q` |
| C5 | 種類フィルタ | Chip「種類 ▼」→ 7 種のダイアログ | 単一選択。選択中は「ワイン ×」 | `drinkType` |
| C6 | 状態フィルタ | Chip「未開栓」「開栓済」 | 排他トグル。無選択 = 両方 | `status` |
| C7 | 棚 | 棚（3 列 / 段） | `createdAt` 降順（新しい本が左上）。段ごとに棚板を描く。最後の段が 1〜2 本でも棚板は横一杯 | `items[]` |
| C8 | ボトル | 写真 2:3 100×150 + 名前 + サブ | 写真: `thumbPhotoId` の画像（角 8px、下端が棚板に接する）。無ければ種類別ボトル型 SVG（`--muted` 線、種類名は下のテキストで） | `GET /api/photos/:id/content` |
| C9 | サブ行 | テキスト 12px | `vintage` があれば年、無ければ「NV」。`opened` はピル「開栓」（primary 文字、inset） | `status`, `vintage` |
| C10 | ボトルタップ | — | `/cellar/:bottleId` | — |
| C11 | もっと読む | — | 下端到達で `nextCursor` を自動取得（棚 2 段ずつ） | `cursor` |

### 状態

| 状態 | 表示 |
|---|---|
| ローディング | 棚 2 段分の枠（100×150 inset ×6 + 棚板） |
| 空（在庫 0） | 棚板 1 本の上に **キャラ `surprised` 96px** が立つ。「ボトルはまだありません。撮って 1 本目を並べましょう」+ Button 主「ボトルを追加」 |
| フィルタ結果 0 | 棚板 1 本 + 「該当するボトルがありません」+ Chip「フィルタを解除」（キャラなし） |
| エラー | 「読み込めませんでした」+ 再試行 |
| 復元直後（undo） | 該当ボトルが 2 秒ハイライト（inset 枠） |

写真は `<img loading="lazy">`。同一オリジンの認可付き GET なので Cookie が付く。

---

## `bottle-archive` 貯蔵庫（アーカイブ）

| ルート | 親タブ | タブバー | ヘッダー | 主アクション |
|---|---|---|---|---|
| `/cellar/archive` | cellar | 表示 | ← 「貯蔵庫 34 本」 | なし（閲覧） |

```
┌──────────────────────────────────┐
│ ←        貯蔵庫  34 本            │
├──────────────────────────────────┤
│ [🔍] [種類 ▼]                     │
│ 2026年9月                          │  月見出し（consumedOn の JST 月）
│  ┌────┐   ┌────┐   ┌────┐       │  写真は saturate(0.6) brightness(0.92)
│  │ 9/5│   │ 9/2│   │ 9/1│       │  右上に消費日ピル
│  └────┘   └────┘   └────┘       │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  サンプル赤  …                     │
│ 2026年8月                          │
│  …                                │
├──────────────────────────────────┤
│ ホーム  セラー* (記録)  ノート  設定│
└──────────────────────────────────┘
```

| # | 要素 | 内容 | API |
|---|---|---|---|
| A1 | 見出し | 「貯蔵庫」+ 総数 | `GET /api/bottles?view=archive` |
| A2 | 月見出し | `consumedOn` の年月で区切る。降順 | `items[]` |
| A3 | ボトル | 棚と同じ 2:3。写真に減彩フィルタ。右上に「9/5」ピル（消費日） | `consumedOn` |
| A4 | タップ | `/cellar/:bottleId`（詳細はアーカイブ表示になる） | — |

空: 「飲み終えたボトルはここに並びます」+ キャラ `default` 96px（主ボタンなし。戻るで棚へ）。

---

## `bottle-new` ボトルを追加

| ルート | 親タブ | タブバー | ヘッダー | 主アクション |
|---|---|---|---|---|
| `/cellar/new?camera=1` | cellar | **隠す** | ← 「ボトルを追加」 | 棚に並べる |

```
┌──────────────────────────────────┐
│ ←  ボトルを追加                    │
├──────────────────────────────────┤
│ ┌ 写真タイル ─┐                    │  撮影前: 📷 撮る（2:3 枠 100×150 左寄せ）
│ │   2:3      │ 撮り直す / 削除     │  撮影後: 加工済みサムネ（cellar プリセット）
│ └────────────┘                    │  キャラ合成トグルは出さない
│ 銘柄名 *                           │
│ [                              ] │  1〜100
│ 種類 *                             │
│ [ワイン*][ビール][ウイスキー]…      │
│ 本数                               │
│  [ − ]  1  [ ＋ ]                  │  1〜12。N 本を並べる
│ 詳細を入力                     ▼  │  折りたたみ（既定閉）
│   生産者 / 産地 / 年 / 購入日 /     │
│   価格 / 購入場所 / 保管場所 / メモ  │
├──────────────────────────────────┤
│ [        棚に並べる（1 本）       ] │  ラベルに本数
└──────────────────────────────────┘
```

### 要素表

| # | 要素 | 種別 | 内容・挙動 | データ / API |
|---|---|---|---|---|
| B1 | 写真タイル | 写真タイル（2:3、左寄せ 100×150） | `photo-edit`（比率 2:3、プリセット `cellar`、**キャラ合成なし**）。`?camera=1` で自動起動。1 枚 | `POST /api/photos` |
| B2 | 銘柄名 | Input | 必須 1〜100 | `name` |
| B3 | 種類 | Chip ×7 | 必須。初期はワイン | `drinkType` |
| B4 | 本数 | ステッパー | 1〜12。**同じ写真・属性で N 行**作る | `count` |
| B5 | 詳細 | 折りたたみ | 生産者・産地（各 ≦100）、年（1800〜2100 / 空 = NV）、購入日（date）、価格（整数円）、購入場所・保管場所（≦100）、メモ（≦2000） | data-model 6.3 |
| B6 | 棚に並べる | Button 主 | 「棚に並べる（N 本）」。無効: 名前空 / 写真アップロード中 | `POST /api/bottles` → 201 `{ items: [...] }` |

### 状態

| 状態 | 表示 |
|---|---|
| `camera=1` | `photo-edit` が先に開く。× で閉じてもフォームは残る（写真なし登録可） |
| 成功 | 作成した最初の本の `bottle-detail` へ `replace`。トースト「棚に並べました」（`cheer`）。N ≥ 2 なら「棚に 3 本並べました」 |
| 失敗 | インライン汎用文 |
| 戻る（未保存） | 入力ありなら確認。写真は削除 |

### バリデーション

| 項目 | 規則 |
|---|---|
| 銘柄名 | 1〜100 |
| 年 | 1800〜2100 の整数、または空 |
| 価格 | 0 以上の整数 |
| 本数 | 1〜12 |
| 購入日 | `YYYY-MM-DD`、未来不可 |

---

## `bottle-detail` ボトル詳細

| ルート | 親タブ | タブバー | ヘッダー | 主アクション |
|---|---|---|---|---|
| `/cellar/:bottleId` | cellar | 表示 | ← 銘柄名 右「編集」 | 消費する（棚のとき）/ ノートを書く（貯蔵庫のとき） |

```
┌──────────────────────────────────┐
│ ←  サンプル赤                 編集 │
├──────────────────────────────────┤
│        ┌──────────────┐           │
│        │              │           │  写真 2:3、高さ最大 300px、中央
│        │    写真      │           │
│        └──────────────┘           │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │  棚板
│ [未開栓]  ワイン ・ 2020 ・ フランス│  状態ピル + 要約 13px
│                                  │
│ [          消費する           ]   │  Button 主
│ [ 開栓する ]  [ 1 杯を記録 ]       │  Button 副 ×2（開栓は sealed のときだけ）
│                                  │
│ 生産者      サンプル生産者          │  プロパティ（値が空の行は出さない）
│ 購入日      2026-06-01             │
│ 価格        ¥3,800                 │
│ 保管場所    リビング               │
│ メモ        …                      │
│                                  │
│ ノート                    書く ›  │  このボトルのノート（最新 3 件 + すべて）
│  2026-08-01  ★4.5             ›  │
│                                  │
│ 記録                              │  このボトルの記録（最新 3 件）
│  9/5 13:05  125ml             ›  │  → log-edit
├──────────────────────────────────┤
│ ホーム  セラー* (記録)  ノート  設定│
└──────────────────────────────────┘
```

### 要素表

| # | 要素 | 種別 | 内容・挙動 | データ / API |
|---|---|---|---|---|
| T1 | 写真 | 写真 2:3（高さ ≦300） + 棚板 | 無ければボトル型 SVG。タップで全画面表示（ピンチ可） | `photos[0]` |
| T2 | 状態ピル + 要約 | ピル + テキスト | 「未開栓」「開栓（9/1）」「消費（9/5）」。要約は 種類 ・ 年 ・ 産地（あるものだけ） | `status`, `openedOn`, `consumedOn` |
| T3 | 消費する | Button 主（棚のとき） | `bottle-consume` ダイアログ | — |
| T4 | 開栓する | Button 副（`sealed` のみ） | 即時 `PATCH { status: "opened", openedOn: today }`。トースト「開栓しました」（`cheer`）。確認なし | `PATCH /api/bottles/:id` |
| T5 | 1 杯を記録 | Button 副 | `/logs/new?bottleId=`（種類・名前が入る） | — |
| T6 | プロパティ | 行リスト | 値が空の行は非表示 | data-model 6.3 |
| T7 | ノート節 | 見出し + 行 + 「書く ›」 | 最新 3 件。「すべて（N）›」→ `/notes?bottleId=`。「書く」→ `/notes/new?bottleId=&camera=1` | `GET /api/tasting-notes?bottleId=&limit=3` |
| T8 | 記録節 | 見出し + 行 | このボトルに紐付く記録 最新 3 件。行 → `log-edit` | `GET /api/drink-logs?bottleId=&limit=3` |
| T9 | 編集 | ヘッダー右 | `/cellar/:bottleId/edit` | — |

### 貯蔵庫（`consumed`）のとき

- T3 は **「ノートを書く」**（Button 主）に置き換える。T4 / T5 は出さない
- ピル「消費（9/5）」の右に「この日の記録 ›」→ `/logs/2026-09-05`
- Button 副「セラーに戻す」を T6 の上に置く。タップで即時 `POST /api/bottles/:id/restore` → 棚へ戻り、トースト「セラーに戻しました」。**記録は消さない**（消費 undo とは別）
- 写真は減彩しない（詳細では原色）

### 状態

| 状態 | 表示 |
|---|---|
| ローディング | 写真枠 + 見出し枠 |
| 404 | `not-found` |
| 開栓直後 | ピルが「開栓（今日）」に変わり、T4 が消える |

---

## `bottle-consume` 消費（ダイアログ）

`bottle-detail` の T3 から。ルートは持たない。

```
┌──────────────────────────────────┐
│ ▓▓▓▓▓▓▓▓▓▓▓▓▓（スクリム）▓▓▓▓▓▓▓▓│
│ ┌ Dialog ─────────────────────┐  │
│ │ [写真48] サンプル赤            │  │
│ │ 消費して貯蔵庫へ移します        │  │  タイトル 20px
│ │                              │  │
│ │ [✓] 今日の記録に 1 杯を追加    │  │  チェック行（既定 ON）
│ │     量  [125*][150][375][750]…│  │  ON のときだけ表示。種類の量チップ + ボトル量
│ │     度数  12 %  [−][＋]        │  │
│ │     日時  いま              › │  │
│ │                              │  │
│ │ [      記録して貯蔵庫へ       ]│  │  Button 主（OFF のとき「貯蔵庫へ移す」）
│ │          キャンセル            │  │  テキスト
│ └──────────────────────────────┘  │
└──────────────────────────────────┘
```

| # | 要素 | 内容 | API |
|---|---|---|---|
| K1 | ボトル要約 | 写真 48px + 銘柄名 | — |
| K2 | 記録チェック | 「今日の記録に 1 杯を追加」。既定 ON。最後の選択を端末に記憶 | — |
| K3 | 量 / 度数 | 種類のデフォルト（[alcohol-calculation.md](../features/alcohol-calculation.md)）。ボトル量チップも出す（丸ごと飲んだ） | — |
| K4 | 日時 | 既定いま。`datetime-local` | `drunkAt` |
| K5 | 主ボタン | ON: 「記録して貯蔵庫へ」/ OFF: 「貯蔵庫へ移す」 | `POST /api/bottles/:id/consume { log: {...} \| null }` |
| K6 | キャンセル | 閉じる | — |

### 成功後

1. ダイアログを閉じる
2. **`/logs/<drunkOn>?highlight=<logId>` へ遷移**（記録 OFF のときは `/cellar` へ戻り、棚から消えたことを見せる）
3. トースト「貯蔵庫へ移しました  取り消す」5 秒。`cheer` 付き
4. 「取り消す」= `POST /api/bottles/:id/restore` → 成功後 `DELETE /api/drink-logs/:logId`（作っていれば）。両方成功でトースト「元に戻しました」。片方失敗は汎用エラー + 画面を再取得

### 状態

| 状態 | 表示 |
|---|---|
| 送信中 | 主ボタン「保存中」無効 |
| 失敗 | ダイアログ内に汎用文。閉じない |
| 度数 0 / 量空 | 主ボタン無効（記録 ON のとき） |

---

## `bottle-edit` ボトルを編集

`/cellar/:bottleId/edit`。`bottle-new` と同じレイアウト。差分:

- 本数ステッパーは無い（1 行 = 1 本）
- 状態は編集できない（詳細のボタンで操作）。開栓日・消費日は詳細から
- 写真の付け替え: 削除 → 撮る。`PATCH` の `photoIds`
- 保存 → `PATCH /api/bottles/:id` → 詳細
- 下部テキストボタン「このボトルを削除」→ 確認ダイアログ「ノートは残ります。記録のボトル名は残ります」（danger）→ `DELETE` → `/cellar`。**貯蔵庫の本も削除できる**

---

## 陳列の写真（4-04 の実装メモ）

| 項目 | 値 |
|---|---|
| 入力 | `photo-edit` の出力（2:3、長辺 1280、`cellar` プリセット適用済み、JPEG） |
| 棚のサムネ | 100×150 表示。`object-fit: cover`。角 8px。下端を棚板の上辺に合わせる |
| 棚板 | 高さ 14px、`--shelf-rail`、下辺 2px `--shelf-rail-edge`、下に `--shadow-outset`。段の間隔 24px |
| 段の構成 | 390px で 3 列（列幅 = (390 − 40 − 16×2) / 3 ≒ 106px）。480px 以上で 4 列 |
| 写真なし | 種類別ボトル型 SVG（ワイン=肩なだらか、ビール=缶、ウイスキー=角瓶、日本酒=一升瓶、焼酎=紙パック風は避け瓶、カクテル=グラス、その他=汎用瓶）。線は `--muted`、塗りは地色 |
| v1.x | 背景除去（ブラウザ WASM、例 `@imgly/background-removal`）で切り抜き → 棚板の上に本物のシルエットで立たせる。モデル配布サイズが大きいため MVP では入れない |

---

## 受け入れチェック（4-02 / 4-03 / 4-04）

- [ ] `/cellar` が **棚 3 列**。写真 2:3 が棚板の上、名前と年 / 開栓ピル
- [ ] 写真なしは種類別ボトル型。空状態は棚板 + `surprised` 96px + 「ボトルを追加」
- [ ] 検索・種類・状態フィルタが動き、貯蔵庫（`consumed`）は棚に出ない
- [ ] 「+」→ 撮影から始まり、× でフォームに戻れる。`cellar` プリセット、キャラ合成トグルなし
- [ ] 本数 N で N 行。「棚に並べる（N 本）」
- [ ] 詳細: 主「消費する」、副「開栓する」（sealed のみ）「1 杯を記録」。ノート節・記録節
- [ ] 消費ダイアログ: 記録チェック既定 ON、量チップ、日時。成功で `log-day?highlight=` + トースト undo（復元 + 記録削除）
- [ ] 貯蔵庫: 月見出し、減彩、消費日ピル。詳細で「セラーに戻す」「この日の記録 ›」
- [ ] `bottle-edit` に削除（確認。ノート・記録は残る）
- [ ] 他人の `:bottleId` は `not-found`
