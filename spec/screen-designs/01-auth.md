# 01 認証（ログイン / サインアップ）

実装: 2-02（Better Auth）/ 2-05（レイアウト）/ 8-03（パスワードリセット）/ 8-04（Google OAuth）/ 8-05（Turnstile）/ 8-06（登録停止）。認証 API は Better Auth の公式クライアントのみ（[../api-design.md](../api-design.md) 2.3）。ボット対策の正本は [../features/rate-limit-abuse.md](../features/rate-limit-abuse.md)。使用量の正本は [../features/usage-monitoring.md](../features/usage-monitoring.md)。

モック: [login.png](../wireframes/mocks/login.png)

---

## `auth-login` ログイン

| ルート | 親タブ | タブバー | ヘッダー | 主アクション |
|---|---|---|---|---|
| `/login` | なし | なし | なし（カード内見出し） | ログイン |

```
┌──────────────────────────────────┐
│                                  │
│           [Mascot default 120]    │
│           [ワードマーク SVG]      │  幅 200px。alt「酒のしおり」
│ ┌ カード ──────────────────────┐ │
│ │ ログイン                      │ │  24px/600
│ │ メール                        │ │
│ │ [                          ] │ │  Input
│ │ パスワード                    │ │
│ │ [                    (表示)] │ │  Input + 表示切替
│ │ [   ボット対策の確認      ]   │ │  Turnstile（キーがあるときだけ）
│ │ [        ログイン         ]   │ │  Button 主
│ │      パスワードを忘れた       │ │  primary 文字リンク → /forgot-password
│ │      アカウントを作成         │ │  primary 文字リンク → /signup
│ │            または             │ │
│ │ [ Googleアカウントでログインする ] │ │  Button 副。L8/L9 は GOOGLE_SIGN_IN_VISIBLE
│ └──────────────────────────────┘ │
│                                  │
└──────────────────────────────────┘
```

### 要素表

| # | 要素 | 種別 | 内容・挙動 | データ / API |
|---|---|---|---|---|
| L1 | キャラクター | Mascot `default` 120px | 装飾（`aria-hidden`）。カード上 24px | — |
| L2 | ワードマーク | SVG 画像 | [wordmark.svg](../assets/brand/wordmark.svg)。幅 `--wordmark-width`（200px）。`alt` は公開名称「酒のしおり」。PNG は置かない。差し替えは SVG ファイルを同じパスで上書きし、色・比率を変えるときは本表とトークンも同じ PR で直す | — |
| L3 | メール | Input `type=email` `autocomplete=email` | 必須。前後空白は trim | — |
| L4 | パスワード | Input `type=password` `autocomplete=current-password` | 必須。右端に表示/非表示（`eye`） | — |
| L5 | ログイン | Button 主 | 押下で無効化 + 「ログイン中」 | `signIn.email` |
| L6 | アカウントを作成 | リンク | `/signup`（`redirect` クエリは引き継ぐ） | — |
| L7 | パスワードを忘れた | リンク | `/forgot-password`。`redirect` は付けない | — |
| L8 | 区切り | テキスト | 「または」。L9 と一緒に出し分ける（`GOOGLE_SIGN_IN_VISIBLE`） | — |
| L9 | Googleアカウントでログインする | Button 副 | **表示中**（`GOOGLE_SIGN_IN_VISIBLE = true`。2026-09-11 復活。文言は「続行」ではなくログインであることが分かる文。2026-09-11）。既存 Google アカウントのみ。新規は作らない。押下で無効化。確認未完了では無効 | `signIn.social`（`provider: google`）。同じ Turnstile トークン |
| L10 | ボット対策 | Turnstile ウィジェット | サイトキーがあるときだけ、パスワードの下・主ボタンの上。常時表示。スキップは置かない。読込失敗は再表示 | `GET /api/config` → `x-captcha-response` |

### 状態

| 状態 | 表示 |
|---|---|
| 初期 | 空のフォーム。メールにフォーカスしない（iOS のキーボード自動表示を避ける）。キーがあるときは確認待ちで送信不可 |
| 送信中 | ボタン無効、「ログイン中」 |
| エラー | カード上部に汎用文「メールまたはパスワードが正しくありません」。どちらが誤りかは出さない |
| 確認読込失敗 | 「確認の読み込みに失敗しました。時間をおいて再度お試しください」。スキップボタンは置かない |
| レート制限 | 「しばらく待ってから試してください」 |
| 再設定後 | `?reset=1` のときカード上部に「パスワードを変更しました。ログインしてください。」 |
| OAuth 失敗 | カード上部に「ログインできませんでした。時間をおいて再度お試しください」。`error` クエリは残さない |
| 成功 | `redirect`（安全な相対パスのみ）または `/` へ `replace`。未確認なら `/age` |

### バリデーション（クライアント。正はサーバー）

- メール: 空 / 形式不正で送信不可
- パスワード: 空で送信不可。文字数の下限は表示しない（存在推測を避ける）

### キャラクター

`default` 120px、常時。エラー時もポーズは変えない（叱らない）。

---

## `auth-signup` アカウント作成

| ルート | 親タブ | タブバー | ヘッダー | 主アクション |
|---|---|---|---|---|
| `/signup` | なし | なし | なし | 登録する |

ログインと同じ型。差分のみ:

| # | 要素 | 内容 |
|---|---|---|
| S1 | 見出し | 「アカウント作成」 |
| S2 | 表示名 | Input、任意、1〜40 文字。Better Auth `name` |
| S3 | メール | 必須 |
| S4 | パスワード | 必須。8 文字以上（Better Auth 既定）。`autocomplete=new-password`。強度メーターは置かない |
| S5 | 登録する | Button 主 → 成功後は自動ログインして `/age`（`redirect` があれば `/age?redirect=`） |
| S6 | ログインへ | リンク → `/login` |
| S7 | 法務同意 | 必須チェック。文言「利用規約とプライバシーポリシーに同意する」。リンクはチェックのラベル外（`/terms?from=signup` / `/privacy?from=signup`）。未チェックでは送信不可。サーバーも拒否 | `acceptedLegal` + `legalVersion`（[../features/legal.md](../features/legal.md)） |
| S8 | 区切り | 「または」。S9 と一緒に出し分ける（`GOOGLE_SIGN_IN_VISIBLE`） |
| S9 | Googleアカウントで登録する | Button 副。**表示中**（L9 と同じフラグ。2026-09-11 復活）。未チェックまたは確認未完了では無効。新規は `/age`。同じメールのパスワードユーザーはリンクしない。`signIn.social`（`requestSignUp: true`）。同じ Turnstile トークン |
| S10 | ボット対策 | Turnstile ウィジェット。規約チェックの下、主ボタンの上。キーがあるときだけ。スキップは置かない | `GET /api/config` → `x-captcha-response` |

- 招待コード欄は置かない（確定）
- 既存メールのエラーも汎用文「登録できませんでした。入力内容を確認してください」（存在推測を避ける）
- 登録停止（`SIGNUPS_CLOSED`）はカード上部「現在、新規登録を停止しています」。招待ではない（8-06。[../features/usage-monitoring.md](../features/usage-monitoring.md)）
- S7 の同意は 20 歳以上であることの表明を含む。専用の生年月日確認は `auth-age`（8-02。[../features/age-verification.md](../features/age-verification.md)）

### 状態

ログインと同じ送信中・確認読込失敗・レート制限に加え:

| 状態 | 表示 |
|---|---|
| エラー | カード上部「登録できませんでした。入力内容を確認してください」。既存メールも同じ |
| 登録停止 | カード上部「現在、新規登録を停止しています」。招待コード欄は出さない。フォームは残す（公開 API に停止フラグは出さない） |
| 成功 | `/age`（`redirect` があれば引き継ぐ）へ `replace` |

---

## `auth-age` 年齢確認

| ルート | 親タブ | タブバー | ヘッダー | 主アクション |
|---|---|---|---|---|
| `/age` | なし | なし | なし（カード内見出し） | 確認する |

ログインと同じ型（`.auth-page`）。未ログインは `/login?redirect=/age`。確認済みが来たら `redirect`（安全な相対パス）または `/`。

```
┌──────────────────────────────────┐
│                                  │
│           [Mascot default 120]    │
│           [ワードマーク SVG]      │
│ ┌ カード ──────────────────────┐ │
│ │ 年齢確認                      │ │
│ │ 酒類の記録のため、20歳以上の  │ │
│ │ 方のみ利用できます。          │ │
│ │ 生年月日                      │ │
│ │ [ 1990 ]年 [ 1 ]月 [ 15 ]日   │ │  数字キーボードの 3 欄。打ち終わると次へ
│ │ 例: 1990 年 1 月 15 日          │ │  13px muted
│ │ [        確認する         ]   │ │
│ │         ログアウト            │ │
│ │       アカウントを削除         │ │  A9。年齢確認の完了は要求しない
│ └──────────────────────────────┘ │
└──────────────────────────────────┘
```

拒否時は同じカードで見出しと本文だけ替える。生年月日は残さない。

### 要素表

| # | 要素 | 種別 | 内容・挙動 | データ / API |
|---|---|---|---|---|
| A1 | キャラクター | Mascot `default` 120px | 装飾（`aria-hidden`）。エラー時もポーズは変えない | — |
| A2 | ワードマーク | SVG 画像 | L2 と同じ | — |
| A3 | 見出し | テキスト | 確認: 「年齢確認」。拒否: 「ご利用いただけません」 | — |
| A4 | 説明 | テキスト | 確認: 「酒類の記録のため、20歳以上の方のみ利用できます。」拒否: 「20歳未満の方は本サービスをご利用いただけません。」飲酒を勧める文言は置かない | — |
| A5 | 生年月日 | `fieldset`「生年月日」+ Input ×3（年 / 月 / 日。`inputmode=numeric` `autocomplete=bday-year / bday-month / bday-day`） | 確認状態のみ。必須。**カレンダー（`type=date`）は使わない**（今日から数十年戻す操作になる。2026-09-10）。年 4 桁で月へ、月 2 桁（または 2〜9 の 1 桁）で日へ、日 2 桁（または 4〜9 の 1 桁）で自動的に次の欄へ移す。全角数字は半角に直し、数字以外は捨てる。1 桁の月日は 0 埋めして `YYYY-MM-DD` に組む。下に「例: 1990 年 1 月 15 日」（13px muted）。1900 年より前・未来・実在しない日は送信不可（入力途中では赤を出さず、全欄が埋まってから「生年月日を確認してください」） | `birthOn` |
| A6 | 確認する | Button 主 | 押下で無効化 + 「確認中」。未満は拒否状態へ。成功は `redirect` または `/` | `POST /api/me/age-verification` |
| A7 | ログアウト | Button 副 / リンク | `endSession`。確認ダイアログは出さない（まだ本機能に入っていない） | Better Auth `signOut` |
| A8 | 生年月日を修正 | Button 副 | 拒否状態のみ。確認フォームへ戻す（再入力可） | — |
| A9 | アカウントを削除 | リンク | 確認状態・拒否状態の両方。`/settings/account/delete`。年齢確認の完了は要求しない。認証と本人確認は削除画面側 | — |

### 状態

| 状態 | 表示 |
|---|---|
| 初期 | 空の 3 欄。フォーカスしない |
| 送信中 | ボタン無効、「確認中」 |
| 形式エラー | カード上部「生年月日を確認してください」（3 欄が埋まって日付にならないとき、またはサーバー 400） |
| 拒否（未満） | A3 / A4 を拒否文に。A5 / A6 を隠し A8 を出す。A9 は確認中・拒否の両方で出す |
| 成功 | `redirect` または `/` へ `replace` |
| 確認済みで再訪 | フォームを出さず `redirect` または `/` |

### バリデーション（クライアント。正はサーバー）

- 空 / 形式不正 / 未来日は送信不可
- 20 歳かどうかの判定はクライアントに頼らない（送ってサーバーが計算する）

### キャラクター

`default` 120px、常時。拒否時も叱らない。

---

## `auth-forgot-password` パスワード再設定（メール）

| ルート | 親タブ | タブバー | ヘッダー | 主アクション |
|---|---|---|---|---|
| `/forgot-password` | なし | なし | なし | 送信する |

ログインと同じ型（`.auth-page`）。`GuestOnly` に入れない。ログイン中でも表示する。

```
┌──────────────────────────────────┐
│           [Mascot default 120]    │
│           [ワードマーク SVG]      │
│ ┌ カード ──────────────────────┐ │
│ │ パスワード再設定              │ │
│ │ 登録したメールアドレスを      │ │
│ │ 入力してください。登録が      │ │
│ │ ある場合は案内を送ります。    │ │
│ │ メール                        │ │
│ │ [                          ] │ │
│ │ [   ボット対策の確認      ]   │ │  Turnstile（キーがあるときだけ）
│ │ [        送信する         ]   │ │
│ │         ログインへ            │ │
│ └──────────────────────────────┘ │
└──────────────────────────────────┘
```

完了後はフォームを隠し、同じカードで完了文だけ出す。

### 要素表

| # | 要素 | 種別 | 内容・挙動 | データ / API |
|---|---|---|---|---|
| F1 | キャラクター | Mascot `default` 120px | 装飾（`aria-hidden`）。完了時もポーズは変えない | — |
| F2 | ワードマーク | SVG 画像 | L2 と同じ | — |
| F3 | 見出し | テキスト | 「パスワード再設定」 | — |
| F4 | 説明 | テキスト | 「登録したメールアドレスを入力してください。登録がある場合は、再設定用の案内を送ります。」完了時は出さない | — |
| F5 | メール | Input `type=email` `autocomplete=email` | 必須。前後空白は trim。完了時は隠す | — |
| F6 | 送信する | Button 主 | 押下で無効化 + 「送信中」。完了時は隠す | `requestPasswordReset`（`redirectTo: /reset-password`） |
| F7 | ログインへ | リンク | `/login` | — |
| F8 | 完了文 | テキスト | 「入力されたメールアドレスに、再設定の案内を送りました。届かない場合は受信トレイと迷惑メールを確認してください。」登録の有無で変えない | — |
| F9 | ボット対策 | Turnstile ウィジェット | メールの下、送信の上。キーがあるときだけ。完了時は隠す。スキップは置かない | `GET /api/config` → `x-captcha-response` |

### 状態

| 状態 | 表示 |
|---|---|
| 初期 | 空のメール。フォーカスしない。キーがあるときは確認待ちで送信不可 |
| 送信中 | ボタン無効、「送信中」 |
| 確認読込失敗 | ログインと同じ読込失敗文。スキップは置かない |
| 完了 | F4 / F5 / F6 / F9 を隠し F8 を出す。未登録でも同じ |
| レート制限 | 「しばらく待ってから試してください」 |
| 通信失敗 | 「送信できませんでした。時間をおいて再度お試しください」 |

### バリデーション（クライアント。正はサーバー）

- メール: 空 / 形式不正で送信不可

### キャラクター

`default` 120px、常時。完了時も変えない。

---

## `auth-reset-password` 新しいパスワード

| ルート | 親タブ | タブバー | ヘッダー | 主アクション |
|---|---|---|---|---|
| `/reset-password` | なし | なし | なし | 変更する |

ログインと同じ型。`GuestOnly` に入れない。到着時に `token` / `error` をメモリへ移し、アドレスバーから消す。

```
┌──────────────────────────────────┐
│           [Mascot default 120]    │
│           [ワードマーク SVG]      │
│ ┌ カード ──────────────────────┐ │
│ │ 新しいパスワード              │ │
│ │ パスワード（8文字以上）       │ │
│ │ [                    (表示)] │ │
│ │ パスワード（確認）            │ │
│ │ [                    (表示)] │ │
│ │ [        変更する         ]   │ │
│ │         ログインへ            │ │
│ └──────────────────────────────┘ │
└──────────────────────────────────┘
```

トークン無し・`error=INVALID_TOKEN` は同じ無効状態。見出し「リンクが無効です」。

### 要素表

| # | 要素 | 種別 | 内容・挙動 | データ / API |
|---|---|---|---|---|
| R1 | キャラクター | Mascot `default` 120px | 装飾。無効時も `default` | — |
| R2 | ワードマーク | SVG 画像 | L2 と同じ | — |
| R3 | 見出し | テキスト | 入力: 「新しいパスワード」。無効: 「リンクが無効です」 | — |
| R4 | 説明 | テキスト | 入力: 「リンクの案内に沿って、新しいパスワードを設定してください。」無効: 「有効期限切れか、すでに使われたリンクです。もう一度メールからやり直してください。」 | — |
| R5 | パスワード | Input `autocomplete=new-password` | 必須。8 文字以上。表示切替あり。無効時は隠す | — |
| R6 | パスワード（確認） | Input `autocomplete=new-password` | 必須。R5 と一致。表示切替あり。無効時は隠す | — |
| R7 | 変更する | Button 主 | 押下で無効化 + 「変更中」。成功は `/login?reset=1` へ `replace` | `resetPassword` |
| R8 | ログインへ | リンク | `/login`。無効時は「再設定メールを送る」→ `/forgot-password` | — |

### 状態

| 状態 | 表示 |
|---|---|
| 初期（トークンあり） | 空の 2 欄。フォーカスしない |
| 送信中 | ボタン無効、「変更中」 |
| 不一致 | 「パスワードが一致しません」 |
| トークン無効 | R3 / R4 を無効文。R5〜R7 を隠す |
| レート制限 | 「しばらく待ってから試してください」 |
| 成功 | `/login?reset=1` |

### バリデーション（クライアント。正はサーバー）

- 8〜128 文字。確認欄が一致しないと送信不可
- 強度メーターは置かない

### キャラクター

`default` 120px、常時。無効時も叱らない。

---

`/reset-password` に Turnstile は置かない（短命トークン）。

---

## 受け入れチェック（2-02 / 2-05 / 8-01 / 8-02 / 8-03 / 8-04 / 8-05 / 8-06）

- [ ] `/login` `/signup` でキャラ `default` 120px とカード 1 枚。ワードマークは SVG（幅 200px、`alt`「酒のしおり」）
- [ ] ログイン成功で `redirect`（相対パスのみ）または `/`
- [ ] エラー文がメール / パスワードのどちらかを特定しない
- [ ] ログイン済みで `/login` に来たら `/` へ
- [ ] 招待コード欄・テーマ切替が無い
- [ ] 本文 16px、Input 高さ 48px、ボタン 52px
- [ ] ノッチ・ホームバー分の safe-area 余白がある（6-05。`.auth-page`）
- [ ] サインアップに規約・PP への必須チェックがあり、リンクから公開ページへ辿れる（8-01）
- [ ] チェック無しでは登録できない。サーバーも拒否する（8-01）
- [ ] `/age` でキャラ `default` 120px とカード 1 枚。見出しは「年齢確認」
- [ ] 生年月日を送り、満 20 歳以上なら `redirect` または `/`。未満は拒否文とログアウト
- [ ] 生年月日は 年 / 月 / 日 の 3 欄で数字キーボードが出る。`type=date` のカレンダーは無い。年 4 桁で月へ、月 → 日へ自動で欄が移る。入力途中では赤を出さない
- [ ] 未確認のログイン済みユーザーがタブ配下に来たら `/age` へ
- [ ] 飲酒を勧める文言が無い。拒否時もキャラのポーズを変えない
- [ ] ログインに「パスワードを忘れた」があり `/forgot-password` へ行く（8-03）
- [ ] `/forgot-password` の完了文が登録の有無で変わらない（8-03）
- [ ] `/reset-password` でトークン無しは無効文。成功後は `/login?reset=1`（8-03）
- [ ] `/forgot-password` `/reset-password` はログイン中でも表示する（8-03）
- [ ] ログインに「Googleアカウントでログインする」、サインアップに「Googleアカウントで登録する」がある。`GOOGLE_SIGN_IN_VISIBLE` で出し分け、いまは両方で出ている（8-04。2026-09-11）。独立ルートは無い
- [ ] サインアップの Google は規約未チェックでは押せない（8-04）
- [ ] OAuth 失敗は汎用文。クエリの `error` を本文に出さない（8-04）
- [ ] 自前の OAuth 実装が無い（Better Auth のみ。8-04）
- [ ] キーがあるとき `/login` `/signup` `/forgot-password` に確認ウィジェットがある。`/reset-password` には無い（8-05）
- [ ] 確認未完了では送信と Google ボタンが無効。スキップボタンは無い（8-05）
- [ ] 読込失敗は再表示できる。確認を飛ばす裏道は無い（8-05）
- [ ] ログイン失敗文は従来の汎用文。確認の成否を列挙しない（8-05）
- [ ] 登録停止中はメール登録も Google 新規も拒否し、「現在、新規登録を停止しています」を出す（8-06）
- [ ] 停止中もログインと既存 Google は使える。招待コード欄は無い（8-06）
- [ ] 年齢確認（確認中・拒否）から「アカウントを削除」で `/settings/account/delete` へ辿れる。削除に年齢確認を要求しない
