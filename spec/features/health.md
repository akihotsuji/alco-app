# 公開エンドポイント: GET /api/health

オーナー承認対象。Phase 0（0-04）で追加した公開 API。認証は付けない。

## 目的

デプロイとローカル起動の死活確認。本文に内部情報を出さない。

## 契約

| 方法 | パス | 認証 | レスポンス |
|---|---|---|---|
| GET | `/api/health` | なし | `{ "ok": true }` |

- スタックトレース、アカウント ID、シークレット、バインディング内部名は返さない
- 未定義の `/api/*` は `{ "ok": false }` の 404（存在推測を避けるため詳細は出さない）

契約は `src/server/index.test.ts` で Hono の `app.request()` により固定する（0-06）。

## 対象外

- 認証付き API（Phase 2 以降。health 以外の公開エンドポイントは増やさない）
- 詳細な readiness（D1/R2 接続確認はここではしない）
