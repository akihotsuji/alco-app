# Phase 4: セラー管理

| 項目 | 内容 |
|---|---|
| 目安 | 1〜1.5週間 |
| 状態 | 未着手 |
| ソース | [spec/03-roadmap.md](../../spec/03-roadmap.md) Phase 4 |

## 目的

**撮って棚に並べ、消費で貯蔵庫へ移す**在庫管理を構築する（2026-09-05 改訂）。写真基盤は Phase 2-08 のものを使う。画面は [spec/screen-designs/04-cellar.md](../../spec/screen-designs/04-cellar.md) のとおりに作る。

```
 追加（撮って登録） → 棚（sealed / opened） → 消費 → 貯蔵庫（consumed）
                                                   └→ その日の記録に 1 杯（任意）→ log-day
```

## 依存

- Phase 2 完了（認証、API 基盤、レイアウト、**写真パイプライン 2-08**）
- Phase 3 完了（消費で drink-log を作るため。`log-day?highlight=` へ遷移）
- 実装前に `spec/features/cellar.md` を承認する（04-cellar.md を写す）

## ゴール（フェーズ DoD）

- 手持ちのボトルを撮って（切り抜かれて）ガラス棚に並べ、種類ごと / 1 本ずつを切り替え、検索・絞り込みでき、消費すると貯蔵庫へ移ってその日の記録が増える
- ラベル写真から候補が空欄に入り、確認して保存できる（読めなくても登録は止まらない）
- 写真が R2 に保存され、他ユーザーからアクセスできないことがテストで担保されている
- 消費の undo でボトルと記録の両方が元に戻る

## タスク一覧

| # | ファイル | 要点 |
|---|---|---|
| 01 | [機能仕様](01-spec-cellar.md) | `spec/features/cellar.md` |
| 02 | [追加・詳細・編集・削除](02-bottle-crud.md) | 撮影から始まる追加（本数 N → N 行）、詳細（主「消費する」）、編集、削除 |
| 03 | [消費・貯蔵庫・開栓・復元](03-status-management.md) | 消費ダイアログ + 記録同時作成、`/cellar/archive`、開栓、セラーに戻す、undo |
| 04 | [陳列（ガラス棚）](04-photo-upload-r2.md) | ガラス棚板、種類ごと / 1 本ずつ、`kind` による描き分け、種類別シルエット |
| 05 | [テスト](05-api-component-tests.md) | API・コンポーネント、認可・トランザクション含む |
| 06 | [切り抜き](06-background-removal.md) | 端末内 WASM で背景除去 → 透過 WebP（`kind = cutout`）。フォールバックあり |
| 07 | [ラベル読み取り](07-label-recognition.md) | Workers AI（Vision）で候補を空欄に。`POST /api/bottles/recognize`、日次上限 |

推奨順: 01（承認）→ 02（棚は仮の一覧でよい）→ 04（棚。まず長方形 `kind = photo`）→ 03（消費・貯蔵庫）→ 06（切り抜き）→ 07（ラベル読み取り）→ 05。06 と 07 は独立していて並行できる。05 は各 PR にテストを含めるのが本則で、本タスクは抜け漏れの総仕上げ。

## スコープ外（このフェーズ）

- 飲み頃メモ・アラート、在庫金額サマリー（v1.x）
- Gemini 等の外部 Vision API（`LabelRecognizer` の差し替えで将来対応）
- バーコード・外部 DB（[spec/00-overview.md](../../spec/00-overview.md) スコープ外）
- ノート作成時の記録同時作成（v1.x）

## 終了後にできること

Phase 5 でノートのボトル選択（貯蔵庫含む）と複数写真に、本フェーズの棚・写真を使う。
